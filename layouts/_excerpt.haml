---
---
.excerpt
  - url = "#{route_path(item)}"
  %h2 
    %a(href="#{url}" class="permalink")= "&laquo; #{item[:title]}"
  .body
    %p
      = item[:excerpt]
  -# = render('_meta', :item => item)
    
